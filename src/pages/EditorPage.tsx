import { GuestSignup } from "../features/auth/components/GuestSignup";

export const EditorPage: React.FC = () => {
    return (
        <div>
            <GuestSignup />
            <h1>Editor Page</h1>
        </div>
    );
};